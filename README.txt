Lab 8 - Morse Encoder

Takes an input of a txt file with a character and it's corresponding morse code translation
and a file to encode into morse code.

Uses a custom map to store and find morse code values.

